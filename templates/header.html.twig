{% block header %}
    <header>
        {# TODO : if user is logged href to dashboard #}
        <h1><a href="{{ path('landing_page') }}">HMS(t)R</a></h1>

        {# TODO: Add condition for hide when no user is logged in #}
        <nav>
            <ul>
                <li><a class="button-base button-main button-with-icon" href="{{ path('rooms_index') }}">
                        {{ source('@icons/classroom-icon.svg') }}
                        <span>Classrooms</span></a></li>
                <li><a class="button-base button-secondary-outline button-with-icon" href="#">
                        {{ source('@icons/reservation-icon.svg') }}
                        <span>Reservation</span></a></li>
                <li><a class="button-base button-secondary-outline button-with-icon" href="#">
                        {{ source('@icons/group-icon.svg') }}
                        <span>Groups</span></a></li>
                <li><a class="button-base button-secondary-outline button-with-icon" href="{{ path('users_index') }}">
                        {{ source('@icons/account-badge-icon.svg') }}
                        <span>Users</span></a></li>
            </ul>
        </nav>

        <section class="user-links">
            <a class="button-base button-main" href="#">User name</a>
            {#
            TODO : once we have JS make it work :]
            <ul>
                User name
                <li><a href="#">Profile</a></li>
                <li><a href="#">Log out</a></li>
            </ul>
            #}
        </section>
    </header>
{% endblock %}