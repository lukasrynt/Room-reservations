{% extends 'header.html.twig' %}

{% block title %} Building detail {% endblock %}

{% block body %}
    <section>
        <h1>{{ building.name }}</h1>
        <h2>Detail</h2>
        <dl>
            <dd>Address</dd>
            <dt>{{ building.address }}</dt>
        </dl>
        <a href={{ path('building_edit', {'id': 'x'}) }}>Edit</a>
    </section>
{% endblock %}